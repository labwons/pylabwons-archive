# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: BUILD LABWONS

on:
  # schedule:
  #   - cron: '31 6 * * 1-5'  # KST 3:31 PM
  #   - cron: '0 13 * * 1-5' # KST 10:00 PM
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Checkout LABWONS repository
      uses: actions/checkout@v4
      with:
        repository: labwons/labwons
        token: ${{ secrets.PAT_LABWONS }}
        path: repo
    
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .

    - name: Build
      env:
        KRX_ID: ${{ secrets.KRX_ID }}
        KRX_PW: ${{ secrets.KRX_PW }}
      run: |
        python run/build_action.py

    - name: Set Git Config
      run: |
        git config --global user.name 'labwons.manager'
        git config --global user.email 'snob.labwons@gmail.com'

    - name: Deploy to LABWONS
      run : |
        cp data/src/html/marketmap.html repo/docs/index.html
        cd repo
        git add .
        
        TIMESTAMP=$(TZ='Asia/Seoul' date +'%Y-%m-%d %H:%M:%S')
        if git diff --cached --quiet; then
          echo "No changes to commit."
        else
          git commit -m "[labwons.machine] on $TIMESTAMP"
          git push
        fi

    - name: Commit and Push changes
      run : |
        git add . -- :!repo/
        
        TIMESTAMP=$(TZ='Asia/Seoul' date +'%Y-%m-%d %H:%M:%S')
        if git diff --cached --quiet; then
          echo "No changes to commit."
        else
          git commit -m "[labwons.machine] on $TIMESTAMP"
          git push
        fi

    
